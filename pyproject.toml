[project]
name = "cafe-des-sim"
version = "0.1.0"
description = "Discrete Event Simulation for a small cafe"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "simpy==4.1.1",
  "openpyxl==3.1.5",
  "matplotlib==3.9.0",
]

[project.scripts]
cafe-sim = "cafe_sim:main"
